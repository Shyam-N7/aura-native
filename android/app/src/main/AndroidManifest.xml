<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lockscreen/notification playback controls on Android 13+ (runtime-requested on first play).
         WAKE_LOCK + FOREGROUND_SERVICE* + the MusicService come from react-native-track-player's
         own manifest via merger. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Queue drag-reorder haptics (pickup/drop ticks). -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- The equalizer: android.media.audiofx.* refuses to construct without this.
         Normal permission — granted at install, no runtime prompt. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- FCM notification branding: the white aura mark as the status-bar
           icon (the launcher's monochrome layer) and the accent as its tint,
           for every push the OS renders on our behalf. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_launcher_monochrome" />
      <!-- tools:replace — react-native-firebase's own manifest declares this
           as @color/white; ours must win the merge. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/push_accent"
        tools:replace="android:resource" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Playlist share links: /p/PUBLIC_ID (public view) and
             /playlists?join=TOKEN (invite). The app shares www links
             (API_BASE = www.aurafm.live), which serves
             /.well-known/assetlinks.json directly with this app's signing
             fingerprint — so www auto-verifies and opens straight into the
             app, no chooser. -->
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
            <data android:host="www.aurafm.live" />
            <data android:pathPrefix="/p/" />
            <data android:pathPrefix="/t/" />
            <data android:path="/playlists" />
        </intent-filter>
        <!-- Bare apex 308-redirects to www, and Digital Asset Links
             verification does not follow redirects — so aurafm.live can't
             auto-verify. Keep it handled (open-with chooser) so apex links
             from web shares still reach the app. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
            <data android:host="aurafm.live" />
            <data android:pathPrefix="/p/" />
            <data android:pathPrefix="/t/" />
            <data android:path="/playlists" />
        </intent-filter>
      </activity>
    </application>
</manifest>
