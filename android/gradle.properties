# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# This machine has ~8 GB RAM and is usually near-full: keep the whole build inside one
# modest JVM (no separate Kotlin daemon), cap parallelism, or the daemon dies with a
# native malloc OOM (seen: hs_err "Chunk::new" with Xmx2048 + kotlin daemon).
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=768m
org.gradle.workers.max=2
kotlin.compiler.execution.strategy=in-process

# NO MACHINE-SPECIFIC PATHS IN THIS FILE. It is tracked, so anything absolute
# here is absolute for every checkout — and two lines used to be, both fatal on
# a Linux runner:
#
#   org.gradle.java.home=D:/Android/jdk-17   -> read at daemon-JVM selection and
#     it OVERRIDES JAVA_HOME, so actions/setup-java was inert and the build died
#     with "Java home supplied is invalid".
#   -Djava.io.tmpdir=D:/Android/tmp (in jvmargs above) -> "java.io.tmpdir is set
#     to a directory that doesn't exist". A separate, independent failure, so
#     fixing only the first left the job red.
#
# Nothing is lost on the Windows box: scripts/env.cmd already sets JAVA_HOME to
# that same JDK (so Gradle's fallback resolves identically), TMP/TEMP to
# D:\Android\tmp, and GRADLE_USER_HOME to D:\Android\gradle-home. Per-machine
# overrides belong there or in %GRADLE_USER_HOME%\gradle.properties — never here.
#
# The heap caps above ARE kept: they are measured, not preference. This machine
# has ~8 GB and Xmx2048 with a separate Kotlin daemon died with a native malloc
# OOM.

# Release signing lives in android/keystore.properties (git-ignored, next to the also-ignored
# aura-release.keystore) — BACK BOTH UP off this machine; Android updates require the same
# signature forever.

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Single known phone, sideload-only: arm64 keeps the APK roughly half the size.
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=false
