# Sentry upload coordinates (docs/perf/01 §3). Safe to commit: the auth token
# is deliberately NOT here — it comes from SENTRY_AUTH_TOKEN at build time,
# read from a file outside the repo.
defaults.url=https://sentry.io/
defaults.org=aura-fm
defaults.project=aura-native
